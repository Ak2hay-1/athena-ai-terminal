"use client";

import { ChartTerminalLayout } from "../components/ChartTerminalLayout";

/** Full Athena Chart terminal page — exclusive to /chart. */
export function AthenaChartPage() {
  return <ChartTerminalLayout />;
}
